# 로컬 개발 전용 — DB 자격증명은 application.yml 의 env var placeholder 가 그대로 동작.
# 이 프로필은 "관찰 가능성" 만 변경: SQL 로그/바인딩 추적 활성화.
spring:
  jpa:
    show-sql: true

logging:
  level:
    com.kbo.summary: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.web: DEBUG
